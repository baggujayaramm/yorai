import { PolicyPage } from '@/components/PolicyPage';

export default function ContentPolicyPage() {
  return <PolicyPage title="Content Policy" intro="Yorai allows respectful student experience and branch-specific context. It does not allow fake/spam content, private data leaks, or unsupported serious allegations." />;
}
